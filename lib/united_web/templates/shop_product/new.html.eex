<h1>New Shop product</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.shop_product_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.shop_product_path(@conn, :index) %></span>
