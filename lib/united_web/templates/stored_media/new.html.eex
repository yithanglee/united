<h1>New Stored media</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.stored_media_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.stored_media_path(@conn, :index) %></span>
