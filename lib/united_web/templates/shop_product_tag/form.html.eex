<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :shop_product_id %>
  <%= number_input f, :shop_product_id %>
  <%= error_tag f, :shop_product_id %>

  <%= label f, :tag_id %>
  <%= number_input f, :tag_id %>
  <%= error_tag f, :tag_id %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
