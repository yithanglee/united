<h1>Edit Live video</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.live_video_path(@conn, :update, @live_video)) %>

<span><%= link "Back", to: Routes.live_video_path(@conn, :index) %></span>
